#include <stdio.h>
/**
*main - prints specific text, gives a newline then fucks off
*void -if im right, all that does is tell it not to grab any variables or anything
*/

int main(void)
{
puts("\"Programming is like building a multilingual puzzle");
return (0);
}
