#include "main.h"
/**
*_abs - computes the absolute value of an integer
*@n: the value to compute
*Return: positive or negative times negative. a classic
*/

int _abs(int n)
{
if (n >= 0)
{
return (n);
}
else
{
return (n * -1);
}
}
