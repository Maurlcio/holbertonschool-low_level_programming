#include "main.h"

/**
*_islower - checks if a letter is lowercase
*@c: yuh
*Return: 0 if false, 1 if not false
*/

int _islower(int c)
{
if (c >= 'a' && c <= 'z')
{
return (1);
}
else
{
return (0);
}
}
