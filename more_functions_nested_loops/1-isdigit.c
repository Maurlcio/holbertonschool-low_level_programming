#include "main.h"

/**
*_isdigit - are you a single number? no? well you will be soon!
*@c: c
*Return: binary moment
*/

int _isdigit(int c)
{
if (c >= '0' && c <= '9')
{
return (1);
}
else
{
return (0);
}
}
